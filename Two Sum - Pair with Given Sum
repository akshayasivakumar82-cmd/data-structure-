class Solution:
    def twoSum(self, arr, x):
        seen = set()
        
        for i in range(len(arr)):
            if x - arr[i] in seen:
                return True
            seen.add(arr[i])
            
        return False
